stages:
    - build
    - imageBuildAndPush
    - deployDev
    - dt-event
    - jmeter-test
    - imageBuildAndPushStaging
    - release
    - deployStag
    - regcred-clean
    - stag-clean
    - deploy-clean
    - deploy-stag-clean

go-build:
    stage: build
    image: dynatraceacm/gobuilder
    script:
        - export GOPATH=$PWD
        - mkdir -p src/github.com/dynatrace-sockshop/user/
        - cp -R ./api src/github.com/dynatrace-sockshop/user/
        - cp -R ./db src/github.com/dynatrace-sockshop/user/
        - cp -R ./users src/github.com/dynatrace-sockshop/user/
        - cp -R ./main.go src/github.com/dynatrace-sockshop/user/
        - cp -R ./glide.* src/github.com/dynatrace-sockshop/user/
        - cd src/github.com/dynatrace-sockshop/user && ls -lsa
        - glide install
        - go build -a -ldflags -linkmode=external -installsuffix cgo -o ./user main.go
        - cd /builds/sockshop/user/
    artifacts:
        paths:
            - "/builds/sockshop/user/src/github.com/dynatrace-sockshop/user/"

dt-event-push:
    stage: dt-event
    dependencies:
        - "deploy-to-dev"
    image: alpine/curl:8.14.1
    script:        
        - |
          curl -X "POST" "https://$DT_TENANT_UUID.live.dynatrace.com/api/v2/events/ingest" --header "accept: application/json; charset=utf-8" --header "Authorization: Api-Token $DT_ACCESS_TOKEN" --header "Content-Type: application/json; charset=utf-8" --data '{ "entitySelector": "type(SERVICE),tag(app:user),tag(environment:dev)", "eventType": "CUSTOM_INFO", "properties": { "Gitlab Build Number": "$CI_PIPELINE_IID", "Git commit": "$CI_COMMIT_MESSAGE" }, "title": "Deployment Event" }'

docker-buildnpush:
  image: docker:27.2.0
  stage: imageBuildAndPush
  dependencies:
    - go-build
  #needs: ["monaco", "init_keptn"]
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: "/certs"
    DOCKER_TLS_VERIFY: 0
    DOCKER_DRIVER: overlay2
    DOCKER_CERT_PATH: "$DOCKER_TLS_CERTDIR/client"
    #important! MTU 1410 makes the MTU packages compatible with the underlying network. Otherwise, it will get stuck in downloads 
    DOCKER_DAEMON_OPTIONS: "--insecure-registry=gitlab-registry.gitlab.svc.cluster.local:5000 --mtu=1410"
  services:
    - name: docker:27.2.0-dind
      entrypoint: [ "sh", "-c", "dockerd-entrypoint.sh $DOCKER_DAEMON_OPTIONS" ]
  before_script:
    - docker info
  script:
    - docker info
    - docker build -t gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:DEV-$CI_PIPELINE_IID .
    - docker login gitlab-registry.gitlab.svc.cluster.local:5000 -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD
    - docker push gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:DEV-$CI_PIPELINE_IID
    - echo "AUTH_TOKEN=$(sed -nE '$!{:a;N;$!ba;s/\n//g;s/"auth":[^"]*"([^"]*)"/\n\1\nAUTH/g};/^[^\n]*\nAUTH/P;D' ~/.docker/config.json)" >> auth.env
    - echo "$CI_PIPELINE_IID"
  artifacts:
        reports:
            dotenv: auth.env

deploy-to-dev:
    dependencies:
        - docker-buildnpush
        - go-build
    stage: deployDev
    image: alpine/kubectl:latest
    script:
        - kubectl create secret docker-registry userregcred --docker-server=gitlab-registry.gitlab.svc.cluster.local:5000 --docker-username=$CI_REGISTRY_USER --docker-password=$AUTH_TOKEN
        - echo "$CI_DEPLOY_USER, $CI_DEPLOY_PASSWORD"
        - kubectl create secret docker-registry userdeploycred --docker-server=gitlab-registry.gitlab.svc.cluster.local:5000 --docker-username=$CI_DEPLOY_USER --docker-password=$CI_DEPLOY_PASSWORD -n dev
        - 'sed -i "s#image: .*#image: gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:DEV-$CI_PIPELINE_IID#" manifest/user.yml'
        - "kubectl -n dev apply -f manifest/user.yml"
        - kubectl wait pod -l app=user  -n dev --for=condition=Ready --timeout=300s
        - kubectl delete secret userregcred
        - kubectl delete secret userdeploycred -n dev

jmeter-tests:
    dependencies:
        - go-build
    needs:
        - deploy-to-dev
        - go-build
    stage: jmeter-test
    image: dynatraceace/jmeter-runner:1.0.0-acl
    script:
        - /jmeter/bin/jmeter.sh -n -t jmeter/basiccheck.jmx -l HealthCheck_user_dev_$VERSION-$CI_PIPELINE_IID -JSERVER_URL=user.dev -JSERVER_PORT=80 -JCHECK_PATH "/health" -JVUCount=1 -JLoopCount=1 -JDT_LTN="HealthCheck_1" -JTestDuration='120' -JThinkTime='250' > Basicresults.txt
        - RESULT=$(awk '/Err:\s*/{print $15}' Basicresults.txt)
        - GOAL="0"
        - echo $RESULT
        - RESULT=$(echo "$RESULT")
        - if [ $RESULT != $GOAL ]; then cat BasicResults.txt && echo "jmeter returned error" && exit 63; fi
        - /jmeter/bin/jmeter.sh -n -t jmeter/user_load.jmx -l FuncCheck_user_dev_$VERSION-$CI_PIPELINE_IID -JSERVER_URL=user.dev -JSERVER_PORT=80 -JCHECK_PATH "/health" -JVUCount=1 -JLoopCount=1 -JDT_LTN="FuncCheck_1" -JTestDuration='120' -JThinkTime='250' > Funcresults.txt
        - RESULT=$(awk '/Err:\s*/{print $15}' Funcresults.txt)
        - echo $RESULT
        - RESULT=$(echo "$RESULT")
        - if [ $RESULT != $GOAL ]; then cat FuncResults.txt && echo "jmeter returned error" && exit 63; fi

docker-buildnpush-staging:
  image: docker:27.2.0
  stage: imageBuildAndPushStaging
  dependencies:
    - go-build
    - jmeter-tests
  rules:
    - if: $CI_COMMIT_TAG  
  #needs: ["monaco", "init_keptn"]
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: "/certs"
    DOCKER_TLS_VERIFY: 0
    DOCKER_DRIVER: overlay2
    DOCKER_CERT_PATH: "$DOCKER_TLS_CERTDIR/client"
    #important! MTU 1410 makes the MTU packages compatible with the underlying network. Otherwise, it will get stuck in downloads 
    DOCKER_DAEMON_OPTIONS: "--insecure-registry=gitlab-registry.gitlab.svc.cluster.local:5000 --mtu=1410"
  services:
    - name: docker:27.2.0-dind
      entrypoint: [ "sh", "-c", "dockerd-entrypoint.sh $DOCKER_DAEMON_OPTIONS" ]
  before_script:
    - docker info
  script:
        - docker login gitlab-registry.gitlab.svc.cluster.local:5000 -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD
        - docker pull gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:DEV-$CI_PIPELINE_IID
        - docker tag gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:DEV-$CI_PIPELINE_IID gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:$CI_COMMIT_TAG-$CI_PIPELINE_IID
        - docker push gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:$CI_COMMIT_TAG-$CI_PIPELINE_IID

release_job:
  stage: release
  dependencies:
        - docker-buildnpush-staging
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  rules:
    - if: $CI_COMMIT_TAG                 # Run this job when a tag is created
  script:
    - echo "running release_job"
  release:                               # See https://docs.gitlab.com/ee/ci/yaml/#release for available properties
    tag_name: '$CI_COMMIT_TAG'
    description: '$CI_COMMIT_TAG'

deploy-to-staging:
    dependencies:
        - docker-buildnpush
        - docker-buildnpush-staging
        - go-build
    stage: deployStag
    rules:
        - if: $CI_COMMIT_TAG
    image: alpine/kubectl:latest
    script:
        - kubectl create secret docker-registry userregcred --docker-server=gitlab-registry.gitlab.svc.cluster.local:5000 --docker-username=$CI_REGISTRY_USER --docker-password=$AUTH_TOKEN
        - echo "$CI_DEPLOY_USER, $CI_DEPLOY_PASSWORD"
        - kubectl create secret docker-registry userdeploycred --docker-server=gitlab-registry.gitlab.svc.cluster.local:5000 --docker-username=$CI_DEPLOY_USER --docker-password=$CI_DEPLOY_PASSWORD -n staging
        - 'sed -i "s#image: .*#image: gitlab-registry.gitlab.svc.cluster.local:5000/sockshop/user:$CI_COMMIT_TAG-$CI_PIPELINE_IID#" manifest/user.yml'
        - 'sed -i "s#namespace: .*#namespace: staging#" manifest/user.yml'
        - kubectl delete pod -l app=user -n staging
        - "kubectl -n staging apply -f manifest/user.yml"
        - kubectl wait pod -l app=user  -n staging --for=condition=Ready --timeout=300s
        - kubectl delete secret userregcred
        - kubectl delete secret userdeploycred -n staging

regcred-cleaner:
    rules:
        - when: on_failure
    stage: regcred-clean
    image: alpine/kubectl:latest
    script:
        - kubectl delete secret userregcred
    allow_failure: true

deploy-cleaner:
    rules:
        - when: on_failure
    stage: deploy-clean
    image: alpine/kubectl:latest
    script:
        - kubectl delete secret userdeploycred -n dev
    allow_failure: true

deploy-stag-cleaner:
    rules:
        - when: on_failure
        - if: $CI_COMMIT_TAG
    stage: deploy-stag-clean
    image: alpine/kubectl:latest
    script:
        - kubectl delete secret userdeploycred -n staging
    allow_failure: true

staging-cleaner:
    rules:
        - when: on_failure
        - if: $CI_COMMIT_TAG
    stage: stag-clean
    image: alpine/kubectl:latest
    script:
        - "kubectl -n staging delete -f manifest/user.yml"
    allow_failure: true