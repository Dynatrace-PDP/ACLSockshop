---
apiapp.kubernetes.io/version: v1
kind: PersistentVolumeClaim
metadata:
  name: user-db-mongodata
  namespace: dev
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
  namespace: dev
  labels:
    name: user-db
    app.kubernetes.io/part-of: sockshop
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      name: user-db
  template:
    metadata:
      labels:
        name: user-db
        app.kubernetes.io/part-of: sockshop
    spec:
      containers:
      - image: mongo:4.4
        name: user-db
        ports:
        - containerPort: 27017
        resources: {}
        volumeMounts:
        - mountPath: /data/db
          name: user-db-mongodata
      restartPolicy: Always
      volumes:
      - name: user-db-mongodata
        persistentVolumeClaim:
          claimName: user-db-mongodata
---
apiapp.kubernetes.io/version: v1
kind: Service
metadata:
  name: user-db
  namespace: dev
  labels:
    app.kubernetes.io/part-of: sockshop
spec:
  ports:
  - name: "27017"
    port: 27017
    targetPort: 27017
  selector:
    name: user-db
---
apiapp.kubernetes.io/version: v1
kind: PersistentVolumeClaim
metadata:
  name: user-db-mongodata
  namespace: staging
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
  namespace: staging
  labels:
    name: user-db
    app.kubernetes.io/part-of: sockshop
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      name: user-db
  template:
    metadata:
      labels:
        name: user-db
        app.kubernetes.io/part-of: sockshop
    spec:
      containers:
      - image: mongo:4.4
        name: user-db
        ports:
        - containerPort: 27017
        resources: {}
        volumeMounts:
        - mountPath: /data/db
          name: user-db-mongodata
      restartPolicy: Always
      volumes:
      - name: user-db-mongodata
        persistentVolumeClaim:
          claimName: user-db-mongodata
---
apiapp.kubernetes.io/version: v1
kind: Service
metadata:
  name: user-db
  namespace: staging
  labels:
    app.kubernetes.io/part-of: sockshop
spec:
  ports:
  - name: "27017"
    port: 27017
    targetPort: 27017
  selector:
    name: user-db
---
apiapp.kubernetes.io/version: v1
kind: PersistentVolumeClaim
metadata:
  name: user-db-mongodata
  namespace: prod
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db
  namespace: prod
  labels:
    name: user-db
    app.kubernetes.io/part-of: sockshop
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      name: user-db
  template:
    metadata:
      labels:
        name: user-db
        app.kubernetes.io/part-of: sockshop
    spec:
      containers:
      - image: mongo:4.4
        name: user-db
        ports:
        - containerPort: 27017
        resources: {}
        volumeMounts:
        - mountPath: /data/db
          name: user-db-mongodata
      restartPolicy: Always
      volumes:
      - name: user-db-mongodata
        persistentVolumeClaim:
          claimName: user-db-mongodata
---
apiapp.kubernetes.io/version: v1
kind: Service
metadata:
  name: user-db
  namespace: prod
  labels:
    app.kubernetes.io/part-of: sockshop
spec:
  ports:
  - name: "27017"
    port: 27017
    targetPort: 27017
  selector:
    name: user-db